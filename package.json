{
  "name": "mindecho",
  "version": "1.0.0",
  "description": "MindEcho — a memory-aware journal built on Gemini, Firebase Auth, Firestore, Secret Manager, and Cloud Run.",
  "type": "module",
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@google-cloud/secret-manager": "^5.6.0",
    "@google/genai": "^1.0.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "firebase-admin": "^12.6.0"
  }
}
